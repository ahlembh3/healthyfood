<header class="site-header">
    <div class="container-site">
        <a class="navbar-brand" href="{{ path('app_home') }}">
            <img src="{{ asset('images/logo.png') }}" alt="Santé & Nature" class="brand-logo">
            <span class="brand-text">Santé & Nature</span>
        </a>

        <button class="nav-toggle" aria-label="Menu navigation" aria-expanded="false" aria-controls="mainNav">
            <span></span><span></span><span></span>
        </button>

        <nav id="mainNav" class="nav-main" role="navigation" aria-label="Navigation principale">
            <a class="nav-link" href="{{ path('app_home') }}">Accueil</a>
            <a class="nav-link" href="{{ path('recette_index') }}">Recettes</a>
            <a class="nav-link" href="{{ path('plante_index') }}">Plantes médicinales</a>
            <a class="nav-link" href="{{ path('tisane_index') }}">Tisanes</a>
            <a class="nav-link" href="{{ path('article_liste') }}">Blog</a>

            {% if is_granted('ROLE_ADMIN') %}
                <a class="nav-link" href="{{ path('admin_dashboard') }}">Tableau de bord admin</a>
                <a class="nav-link" href="{{ path('app_logout') }}">Déconnexion</a>
            {% elseif app.user %}
                <a class="nav-link" href="{{ path('utilisateurs_dashboard') }}">Mon tableau de bord</a>
                <a class="nav-link" href="{{ path('app_logout') }}">Déconnexion</a>
            {% else %}
                <a class="nav-link" href="{{ path('app_login') }}">Connexion</a>
                <a class="nav-link" href="{{ path('app_inscription') }}">Inscription</a>
            {% endif %}
        </nav>
    </div>
</header>
